<?xml version="1.0" encoding="utf-8"?>
<!--
  取舍:保留 allowBackup="false" 的原意——不参与云备份与设备迁移。
  显式排除会话 token 存储(docpilot-session,EncryptedSharedPreferences):其密钥在
  Android Keystore 中不可迁移,备份/迁移后本就无法解密,排除可避免恢复出无效残留。
  Room 高亮库与 PDF 缓存可随时从服务端重建,同样不值得备份。
-->
<data-extraction-rules>
  <cloud-backup>
    <exclude domain="sharedpref" path="docpilot-session.xml" />
    <exclude domain="database" path="." />
    <exclude domain="root" path="." />
  </cloud-backup>
  <device-transfer>
    <exclude domain="sharedpref" path="docpilot-session.xml" />
    <exclude domain="database" path="." />
    <exclude domain="root" path="." />
  </device-transfer>
</data-extraction-rules>
